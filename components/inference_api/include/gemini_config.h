#ifndef _GEMINI_CONFIG_H
#define _GEMINI_CONFIG_H

// DO NOT CHECK IN THE FOLLOWING LINE
#define GEMINI_API_KEY ""
#define GEMINI_URL "https://generativelanguage.googleapis.com/v1beta/models/gemini-3-flash-preview:generateContent"

#define TEST_PROMPT "A Gold code, also known as Gold sequence, is a type of binary sequence used in telecommunications (CDMA)[1] and satellite navigation (GPS).[2] Gold codes are named after Robert Gold.[3][4] Gold codes have bounded small cross-correlations within a set, which is useful when multiple devices are broadcasting in the same frequency range. A set of Gold code sequences consists of 2n + 1 sequences each one with a period of 2n − 1. "
#define SYSTEM_INSTRUCTION "You are a first-year university engineering student. Does the following make sense to you? Answer yes or no only, do not give any additional context."

#endif